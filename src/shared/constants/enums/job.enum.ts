export enum EJobStatus {
  PENDING = "pending",
  OPEN = "open",
  CLOSED = "closed",
  REJECTED = "rejected",
  EXPIRED = "expired",
}

export enum EJobType {
  FULL_TIME = "full_time",
  PART_TIME = "part_time",
  INTERNSHIP = "internship",
}
