export enum EUserRole {
  ADMIN = "admin",
  JOB_SEEKER = "job_seeker",
  RECRUITER = "recruiter",
}

export enum EUserStatus {
  ACTIVE = "active",
  UNVERIFIED = "unverified",
  LOCKED = "locked",
}
